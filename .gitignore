node_modules/
dist/
*.log
vite.config.js.timestamp-*

# Android TWA project: generated by Bubblewrap from twa-manifest.json.
# Keep the manifest so `bubblewrap update && bubblewrap build` can
# regenerate everything else (see docs/android-apk-plan.md).
android/*
!android/twa-manifest.json
*.docx
